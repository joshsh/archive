//for programming via the graphics interface.
//note: replace the cin's and cout's when inputString has been programmed.

void interProgramMain(){
  int boxes, stops;  //the number of box1, box2, ... , stop1, stop2, ...
  
  
  //write the program-------------------------------------------------------

  char mainName[20];
  char progName[20];
  cout << endl << "What will be the name of the function? "; cin >> mainName;
  cout << endl << "And of the program itself (with .h)? "; cin >> progName;
  
  fstream fileA;
  fstream.open( progName, ios::out );
  
  fileA.put("void ");
  fileA.put( mainName );
  fileA.put( "(){ /n" );
  fileA.put( "  int " );
  for (int ii=1; ii<=boxes; ii++){
    fileA.put( "box" );
    fileA.put( 
  };
  

}