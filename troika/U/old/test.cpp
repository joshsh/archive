#include "U.h"

#include <SDL/SDL.h>

#include "include/graphics/paper.h"
#include "include/graphics/paper.c"

int main()
{
/*
  ifstream in ("patterns");
  universe *u = new universe(in);
  in.close();
  ofstream out ("patterns2.U");
  //*/

/*
  universe *u = new universe("testing");
  list *l = new list(u);
  ascii *a = new ascii(u, "this");
  l->enqueue((item *)a);
  a = new ascii(u, "that");
  l->enqueue((item *)a);
  a = new ascii(u, "the other");
  l->enqueue((item *) a);
  ((item *) a)->remove();
  ofstream out ("testing.U");
  //*/



/*
  u->serialize(out);
  out.close();
  delete u;
  //*/

}
