./cleanup
./bootstrap
./configure

make

make dist
make install
make doxygen-doc


################################################################################
# Cross-compiling libxml2

   export CC=your compiler
   ./configure with proper options
   make
   make install


################################################################################
# Cross-compile Phase2 for ARM.

# First, cross-compile libxml2 and place the ARM libaries into:
#     /opt/Embedix/tools/arm-linux/lib

# I haven't tested whether this is absolutely necessary.  If you leave it out,
# the binaries won't execute on x86... but I don't know whether they do execute
# on ARM.
export CC=/opt/Embedix/tools/arm-linux/bin/cc

# Note: rationale behind also specifying --build:
#     http://www.gnu.org/software/autoconf/manual/autoconf-2.57/html_node/autoconf_164.html
./configure --host=arm-linux --build=i686-pc-linux-gnu --prefix=/opt/Embedix/tools/arm-linux/

make

# Look in here for the ARM executables and libraries:
#     src/.libs/


::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::


%destructor

libtool --mode=execute gdb src/phase2

Qt4/C++

$(LIBOBJS) and $(ALLOCA)

     ./configure CC=c89 CFLAGS=-O2 LIBS=-lposix

./configure --host=arm-linux --with-linux=/opt/Embedix/tools/arm-linux/include

The linker also needs to be arm-linux-ld not ld.
